#include <iostream>
using namespace std;
int missingElement(int arr[], int size)
{
    int start = 0;
    int end = size - 1;
    while (start <= end)
    {
        
    }
}
int main(int argc, char const *argv[])
{
    int arr[12] = {1, 2, 3, 4, 5, 6, 8, 9, 10, 11, 12, 13};
    int size = 12;
    int ans = missingElement(arr, size);
    cout<<ans;

    return 0;
}
