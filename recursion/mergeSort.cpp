#include<iostream>
#include<vector>
using namespace std;
vector<int> mergeSort(vector<int> a, int mid){
    vector<int> ax ;
    vector<int>b;
    vector <int>ans;
    if (ax.size() <= 2)
    {
        
    }
    
}
int main(int argc, char const *argv[])
{
   vector <int> arr = {3,5,7,6,9,1};
    int mid = 2;
    vector <int> ans = mergeSort(arr,mid);
    return 0;
}
